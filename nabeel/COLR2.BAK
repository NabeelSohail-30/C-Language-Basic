#include <stdio.h>
#include <conio.h>

void main(void)
{

	int a;
	textbackground(BLACK);
	textcolor(WHITE);
	clrscr();
	textbackground(2);
	textcolor(RED);
	cprintf("my name is nabeel");

	gotoxy(30,10);
	textbackground(YELLOW);
	textcolor(BLUE);
	cprintf("name is sohail");

	textbackground(WHITE);
	textcolor(BLACK);
	gotoxy(50,10);
	cprintf("          ");
	gotoxy(50,10);
	cscanf("%d",&a);

	getch();

}